#!/bin/sh

sourceDir=~/.aws
targetFile=~/Library/Mobile\ Documents/com\~apple\~CloudDocs/backups/aws/aws-$(date +"%Y_%m_%d_%H_%M_%S").tar.gz

echo "=> AWS: $targetFile"
tar cvf "$targetFile" "$sourceDir"
