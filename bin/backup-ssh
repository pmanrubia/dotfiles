#!/bin/sh

sourceDir=~/.ssh
targetFile=~/Library/Mobile\ Documents/com\~apple\~CloudDocs/backups/ssh/ssh-$(date +"%Y_%m_%d_%H_%M_%S").tar.gz

echo "=> SSH: $targetFile"
tar cvf "$targetFile" "$sourceDir"
